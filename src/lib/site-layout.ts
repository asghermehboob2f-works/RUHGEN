/**
 * Shared horizontal rhythm for marketing pages, navbar, and footer.
 * Wider than legacy 1400px caps so compositions can use the full viewport.
 */
export const SITE_CONTAINER =
  "mx-auto w-full max-w-[min(100%,1680px)] px-4 sm:px-6 lg:px-10 xl:px-14 2xl:px-20";

/**
 * Narrow header + footer bar — slim, proportional, edges line up.
 */
export const SITE_CHROME_SLIM =
  "mx-auto w-full max-w-[min(100%,1020px)] px-4 sm:px-5 lg:px-7";
