/** Served from /public — keep in sync with ./media/img/logo.png when you update the asset */
export const BRAND_LOGO_SRC = "/media/img/logo.png";

/** Intrinsic pixel size for Next/Image (aspect ratio); real file scales with `object-contain`. */
export const BRAND_LOGO_INTRINSIC = { width: 512, height: 512 } as const;
